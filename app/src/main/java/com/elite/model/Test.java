package com.elite.model;

import java.util.Collections;
import java.util.Comparator;
import java.util.List;

/**
 * Created by anil on 16/11/17.
 */

public class Test {

    public Test(String a) {
    }

    public Test() {
    }


/*    private void shortList() {
        Collections.sort(chatHistoryBeanList, new Comparator() {
            @Override
            public int compare(Object o, Object t1) {
                if (a1.time == null || a2.time == null) return -1;
                else {
                    Long long1 = Long.parseLong(String.valueOf(a1.time));
                    Long long2 = Long.parseLong(String.valueOf(a2.time));

                    return long2.compareTo(long1);

                }
            }
        });
    }*/

    /*private void shortList() {
        Collections.sort(chatHistoryBeanList, new Comparator() {
            @Override
            public int compare(ChatHistoryBeanFrList a1, ChatHistoryBeanFrList a2) {
                if (a1.time == null || a2.time == null) return -1;
                else {
                    Long long1 = Long.parseLong(String.valueOf(a1.time));
                    Long long2 = Long.parseLong(String.valueOf(a2.time));
                    return long2.compareTo(long1);
                }
            }
        });

    }*/


}